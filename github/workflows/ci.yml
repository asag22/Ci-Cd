name: Build i Test

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build
      run: cargo build --verbose
    - name: test
      run: cargo test --verbose
